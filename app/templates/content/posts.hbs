{{page-title "Posts"}}
	<h1 class="text-2xl">{{this.model.title}}</h1>
	{{#each this.model.posts as |post|}}
		<LinkTo @route="post" @model={{post.id}}>
			<Posts::Post @title={{post.title}} @walletId={{post.walletId}} @/>
		</LinkTo>
	{{/each}}
{{outlet}}