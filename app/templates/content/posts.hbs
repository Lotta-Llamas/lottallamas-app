{{page-title "Posts"}}
	{{#each this.model as |post|}}
		<LinkTo @route="post" @model={{post.id}}>
			<Ui::Content::PostItems @title={{post.title}} @walletId={{post.walletId}} @/>
		</LinkTo>
	{{/each}}
{{outlet}}